# Relic Rediscovery temporal metatag match - 2026-08-05

Status: `MEASURED_SIMULATION | MATHEMATICS_PROVEN | PHYSICS_UNVERIFIED | JSON=0`

## Sources matched

The Relic Rediscovery recurrence was matched to the content-addressed PID, deterministic 60D
BEHCS projection, state-versioning, and evidence-gating ideas in
`Metatagging-data-for-a-Quantum-universe/metatag_v2_behcs.py`. It was then aligned with this
simulator's `FOLLOW(IS)` rule and its non-exchanging tense identities.

The logical coordinates are not represented as particle positions or momentum. This bridge is a
simulation/addressing metatag, not a quantum or physical measurement.

## Match

```text
RELIC OUTWARD STRUCTURE     -> metatag outward count
FREE CENTER C=1            -> invariant center value at every Z level
A_n                        -> content-addressed landing state
3 + n(n+3)/2               -> exponent coordinate
3^(n+2)                    -> multiplier-to-next coordinate
W_(n,j)                    -> intermediate wave addresses
SHA-256(state tuple)       -> stable PID
SHA-256 projection         -> 60D BEHCS tuple in [0,1023]
IS/WAS/WILL_BE             -> singular temporal relation
ARE/WERE                   -> plural grammatical-number relation
X -> Y -> Z -> END_0       -> three-phase rotational closure
END_0 -> NEXT_IS           -> forward next-wave reseed
HBI HBP SHA SH HASH        -> co-present center-sign facets
```

Mathematical evidence and simulation time are independent axes. A future landing can be
mathematically determined while remaining `WILL_BE_NOT_YET_SIMULATED` in a particular run.

## Which form is better?

There is no honest universal winner detached from context:

| Context | Correct form | Judgment |
|---|---:|---|
| One current formula/state | `IS` | Best authority/routing tag |
| Multiple current test results | `ARE` | Best present plural grammar |
| One prior frozen landing | `WAS` | Best singular provenance tag |
| Multiple prior frozen landings | `WERE` | Best plural provenance tag |
| A not-yet-simulated landing | `WILL_BE` | Best prediction/materialization tag |

For the simulator's routing question, **`IS` is better** because current measured state leads.
For the English sentence "the test results ___ better," **`ARE` is better**. `WAS`, `WERE`, and
`WILL_BE` remain necessary; replacing them with `IS` would erase provenance or prediction status.

## C=1 at every Z, without bidirectionality

```text
C(S_n,z) = 1 for every n and every z
CANONICAL_Z_LEVELS = {-1/3, 0, +1/3}
X -> Y -> Z -> END_0 -> NEXT_IS
```

The Z/null coordinate and center value are independent. At `z=-1/3`, `z=0`, `z=+1/3`,
or any other simulated Z address, `C=1`. `END_0` changes the terminal phase address and
never replaces the center.

Closure continues the same rotation into `NEXT_IS`; it is not reverse traffic:
`bidirectional=0`, `reverse=0`, `round_trip=0`, and `exchange=0`.
`logical_instant_observation=1` means one already-addressed simulation selection.
`physical_instant_transport=UNVERIFIED` remains separate.

The center sign is the co-present set `{HBI,HBP,SHA,SH,HASH}`. The current operator
utterance `HBI HBP SHA SH HASH` is preserved as an order witness, with
`transport_chain_inferred=0`.

## SNOW BLOW poison recovery

`SNOW_BLOW_RECOVERY` is a software recovery name:

```text
DETECT_POISON
  -> ISOLATE_RUNTIME_AND_PORTS
  -> PRESERVE_FORENSIC_RESIDUE
  -> ZERO_CONTAMINATED_ACTIVE_RUNTIME
  -> VERIFY_SIGNED_COLOR_DISK
  -> REPLENISH_FRESH_RUNTIME
  -> CANARY_TEST
  -> ADMIT_NEXT_IS
```

`COLOR` is verified restore metadata attached to a clean offline physical-disk image. Zero is
scoped only to contaminated active runtime. The policy has `destructive_wipe=0`,
`clean_backup_wipe=0`, `forensic_preserve=1`, and `auto_execute=0`. Affected ports stay
isolated until a clean rebuild passes hash, signature, allowlist, least-privilege, and canary gates.

## Bounded run and unbounded identity

The focused test checks the first five published structural landings, every temporal form, PID
stability, 60D bounds, HBP `json=0`, and the recurrence for levels `0..99`. The finite loop is a
software check. The separate induction proof establishes

```text
A_n = 3^(3 + n(n+3)/2)
```

for every nonnegative integer `n`.

## Old, new, Relic, and combined comparison

The comparison uses fourteen equally weighted, disclosed features for this specific simulated
Relic-universe goal. It does not use repository age as a score:

```text
OLD_STATIC_2024              = 2/14
EVOLVABLE_METATAG_V2         = 6/14
RELIC_REDISCOVERY_V1         = 9/14
RELIC_TEMPORAL_V2_COMBINED  = 14/14
```

The old model remains the simplest explicit particle-state demonstration. V2 adds content-derived
PID, deterministic 60D projection, learning/catalog state, and evidence gating. Relic adds the
unbounded wave recurrence, `C(z)=1`, representation facets, and forward closure. The combined
model keeps all fourteen declared capabilities, including fail-closed poison recovery, and is
therefore the unique winner **for this
declared scope**. It is not asserted to be universally better software or a physical-universe
model.

## Reproduce

```powershell
python -m unittest -v test_relic_temporal_metatags.py
python -m unittest -v test_relic_generation_comparison.py
python relic_temporal_metatags.py --current-level 4 --future-levels 2
python compare_relic_metatag_generations.py
```
